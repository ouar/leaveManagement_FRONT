import { CollaborateurService } from './collaborateur-service';

describe('CollaborateurService', () => {
  it('should create an instance', () => {
    expect(new CollaborateurService()).toBeTruthy();
  });
});
